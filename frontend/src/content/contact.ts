/**
 * 연락처 상수.
 *
 * ⚠️ **이 번호는 청년교회 직통이 아니라 모교회(김해교회) 대표번호다**
 * (`docs/PLAN.md §1.4`). 화면에 쓸 때는 "교회 사무실로 연결된다"는 안내를
 * 함께 두어야 한다 — 그러지 않으면 전화를 건 사람이 당황한다.
 * 청년부 직통이 생기면 여기만 고치면 된다.
 *
 * 푸터·승인 대기 화면에 같은 문자열이 각자 박혀 있었고 `/contact`가 세 번째
 * 사용처였다. 번호는 바뀔 수 있는 값이라 갈라두면 한 곳만 고치고 나머지를
 * 놓친다 (`CONVENTIONS.md §2` — 재사용 시점에 모은다).
 */
export const CHURCH_PHONE = "055-333-6321";

/** `tel:` 링크용 — 하이픈을 뺀 형태가 더 널리 동작한다 */
export const CHURCH_PHONE_TEL = CHURCH_PHONE.replace(/-/g, "");
