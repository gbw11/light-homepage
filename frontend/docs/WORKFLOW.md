# 프론트엔드 작업 워크플로우 — 하네스 엔지니어링 루프

이 저장소의 프론트엔드 작업은 전부 이 방식으로 진행한다. 핵심은 두 가지뿐이다:
**① 작업을 눈에 보이는 최소 단위로 쪼갠다. ② 단위마다 검증을 통과해야 다음 단위로 넘어간다.**
검증되지 않은 코드 위에 다음 코드를 쌓지 않는다.

---

## 1. 작업 쪼개기

하나의 "단위"는 **화면에서 눈으로 확인 가능한 최소 변화**다. 기능 전체를 한 번에
구현하지 않는다.

**분해 기준 (기능 하나를 이 순서로 쪼개는 게 기본형):**
1. 레이아웃 뼈대 (정적 마크업, 데이터 없음)
2. 정적/더미 데이터로 렌더 (props로 하드코딩한 값 하나 흘려보기)
3. `@/lib/api` mock 연동 (실제 데이터 흐름, `NEXT_PUBLIC_USE_MOCK=1`)
4. 로딩 / 빈 상태 / 에러 상태
5. 인터랙션 (클릭, 폼 제출, 페이지네이션 등)
6. 접근성 확인 (키보드 포커스, 반응형)

모든 기능이 이 6단계를 다 거치는 건 아니다 — 예: 정적 페이지는 1~2단계로 끝난다.
**기준은 "이 커밋 하나가 30~60분 안에 끝날 만큼 작은가"**다. 안 그러면 더 쪼갠다.

각 단위는 **별도 커밋**으로 남긴다. 커밋 로그가 작업을 어떻게 쪼갰는지 그대로
보여줘야 한다.

---

## 2. 단위별 검증 루프 (순서 고정, 스킵 금지)

```
구현 → lint → type-check → 화면에서 눈으로 확인 → (통과) → 커밋 → 다음 단위
                                              → (실패) → 이 단위 안에서 고친다
```

```bash
npm run lint          # ESLint
npm run type-check    # next typegen && tsc --noEmit
npm run dev           # 브라우저에서 직접 렌더 확인 (mock 모드 기본)
```

- **세 개 다 통과하기 전에는 절대 다음 단위로 넘어가지 않는다.** "나중에 고치기"는
  없다 — 다음 단위가 이 단위 위에 쌓이는 순간 실패 원인을 찾기가 기하급수적으로
  어려워진다.
- 화면 확인은 텍스트로 "구현했다"고 끝내지 않는다. 실제로 `npm run dev`를 띄워서
  브라우저로 본다. 이미지/컴포넌트가 있으면 스크린샷으로 남긴다.

---

## 3. 커밋 / 푸시 기준

| 단계 | 행동 |
|---|---|
| 단위 하나 완료 (검증 통과) | 그 자리에서 커밋 |
| 기능 전체(여러 단위) 완료 | [`../../docs/TESTING.md`](../../docs/TESTING.md) 체크리스트로 최종 확인 → push |
| push 후 | Jenkins/GitHub Actions CI 결과 확인 (`docs/CICD.md` §4 — ❌면 머지하지 않음) |

`frontend_develop`에 직접 커밋하지 않는다 — `feat/fe-*` 브랜치에서 작업하고 PR로
올린다 (`README.md` 브랜치 전략).

---

## 4. 막혔을 때

- 검증 루프에서 실패 → **그 단위 안에서** 고친다. 원인이 이전 단위에 있다면
  그 단위로 되돌아가서 고친 뒤 다시 앞으로 진행한다 (건너뛰지 않는다).
- 스펙 자체가 모호하거나 [`SPEC_FUNCTIONAL.md`](../../docs/SPEC_FUNCTIONAL.md)/
  [`WIREFRAME.md`](../../docs/WIREFRAME.md)와 충돌한다면, 코드로 임의로
  때우지 않고 **먼저 PM(사용자)에게 확인**한다.

---

## 5. 어떤 문서를 언제 참고하는가 (문맥 정보 선택)

문맥 정보는 아무거나 다 끌어오지 않는다 — **신뢰도 · 최신성 · 범위**로 고른다.

| 기준 | 우선순위 |
|---|---|
| 신뢰도 | `docs/SPEC_API.md`(백엔드 계약) > `SPEC_FUNCTIONAL.md`/`WIREFRAME.md`(기획 스펙) > 과거 대화·메모리(참고용, 항상 최신 문서로 재확인) |
| 최신성 | 코드(실제 구현) > 문서 > 기억. 문서와 코드가 다르면 **먼저 어느 쪽이 최신인지 확인**하고, 애매하면 사용자에게 묻는다 |
| 범위 | 지금 단위에 필요한 문서만 연다 — 관련 없는 스펙 전체를 다 읽고 시작하지 않는다 (`CLAUDE.md` 내비게이션 표가 "언제 읽는가"를 이미 분류해둠) |

## 6. 반복 종료 조건 · 실행 상태 · 재개 (체크포인트)

- **커밋 = 체크포인트.** 각 단위가 검증을 통과해 커밋된 지점이 "안전하게
  되돌아갈 수 있는 지점"이다.
- **반복 종료 조건**: 한 단위 안에서 같은 수정을 반복 시도하는 경우, 아래 중
  하나를 만나면 반복을 멈춘다 (`OBSERVABILITY.md` §2와 동일 기준):
  - 검증 루프 2회 연속 실패 → 원인 재진단 또는 사용자에게 확인
  - 같은 에러가 형태만 바뀌어 재발 → 접근 방식 자체를 의심
  - 스펙 자체가 모호해서 "구현"의 정의가 불확실 → 사용자에게 확인 (임의로
    확장 해석하지 않는다)
- **작업이 중단됐다가 재개될 때**: 마지막 성공 커밋(`git log`)과 현재
  `git status`를 먼저 확인한다. 검증을 통과하지 않은 상태의 변경이 남아있으면
  그 단위를 처음부터 다시 검증하고, 이미 커밋된 단위는 다시 손대지 않는다
  (중복 실행 방지).

## 7. 역할 분리 · 백엔드로의 업무 인계

- 프론트 작업 중 **백엔드 계약이 없거나 모호한 지점**을 만나면: 코드를
  임의로 추측해서 만들지 않고, `@/lib/api` mock으로 먼저 화면을 완성한 뒤
  (`CONVENTIONS.md` §3), 필요한 API 형태(요청/응답 스키마)를 정리해서
  PM(사용자)에게 전달한다 — PM이 백엔드 담당자에게 작업을 할당하는 주체다.
- 인계할 때 최소한 포함할 것: 엔드포인트 경로(제안), 요청/응답 필드,
  권한 요구사항(`ARCHITECTURE.md §5.3` 인가 매트릭스에 추가할 행), 왜
  필요한지(어느 화면/기능).

## 8. 단위 시작 전 자문 체크리스트

- [ ] 이 단위는 30~60분 내로 끝날 만큼 작은가?
- [ ] 끝내면 화면에서 눈으로 다른 무언가가 보이는가?
- [ ] 관련 스펙(`SPEC_FUNCTIONAL.md`/`WIREFRAME.md`)을 확인했는가?
- [ ] 코드 작성 규칙은 [`CONVENTIONS.md`](CONVENTIONS.md), 컴포넌트 패턴은
      [`COMPONENTS.md`](COMPONENTS.md)를 따르고 있는가?
