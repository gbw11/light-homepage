# 프로젝트 브리핑 — 김해교회 청년교회 LIGHT 홈페이지

> 이 문서는 외부 AI(이미지 생성·디자인 도구)에게 프로젝트를 설명하기 위한
> 브리핑입니다. 와이어프레임·API 명세 시각화 이미지를 만드는 데 필요한 맥락을
> 담았습니다. 저장소 문서(`docs/spec/WIREFRAME.md` v1.7 · `docs/spec/SPEC_FUNCTIONAL.md`
> v1.2 · `docs/spec/SPEC_API.md` v1.2)에서 추린 요약이며, 충돌 시 저장소 문서가 기준.

---

## 1. 프로젝트가 무엇인가

**대한민국 경남 김해에 있는 교회의 청년부(청년교회 "LIGHT") 공식 홈페이지.**
상업 서비스가 아니라 공동체 사이트다. 목적은 세 가지:

1. **처음 오는 사람(새가족)이 "가도 되겠다"는 확신을 얻게 한다** — 예배 시간·장소·가는 길·분위기
2. **기존 청년(회원)이 매주 다시 올 이유를 준다** — 주보, 사진첩, 월례회 자료
3. **임원·전도사의 운영 업무를 줄인다** — 공지 작성, 자료 업로드, 회원 승인, 출석부

- 예배: **주일 14:00 · 드림센터 4층** (본당과 별개 건물 — 길 안내가 중요한 이유)
- 규모: 청년 수십 명 규모. 트래픽이 아니라 **비용 $0 운영**이 제약
- 언어: **전부 한국어.** UI 문구는 존댓말, 따뜻하고 환대하는 톤 ("등록 없이 오셔도 맞이합니다")
- 모바일 우선: 방문자 대부분이 스마트폰. PWA 지원(홈 화면 설치)

## 2. 기술 스택 (참고용)

- **프론트엔드**: Next.js 15 (App Router) + TypeScript + Tailwind CSS 4 · Vercel 배포
- **백엔드**: Spring Boot 3.5 (Java 21) + PostgreSQL(Neon) · Render 배포 · REST API
- 저장소: 모노레포 (`frontend/` + `backend/`)
- 현재 상태: FE는 전 화면 mock으로 완료, BE는 인증(M2)까지 구현 중. 아직 미공개(SSO 보호 중)

## 3. 디자인 시스템 (이미지 제작 시 이 값을 그대로 쓸 것)

**전체 인상: 따뜻한 베이지 종이 위의 짙은 녹색 잉크.** 차가운 SaaS 느낌이 아니라
교회 소식지 같은 온기. 노랑·파랑 계열 아님에 주의.

### 색 (라이트 모드)
| 용도 | 값 |
|---|---|
| 배경 | `#f6f1e4` (따뜻한 베이지) |
| 본문 텍스트/잉크 | `#16281d` (거의 검정에 가까운 짙은 녹색) |
| 보조 텍스트 | `#635e50` (웜 그레이) |
| 경계선·구분선·보조 배경 | `#e8dfc8` (연한 베이지) |
| **포인트/CTA 버튼** | `#2f7a4a` (**초록**) — 버튼 글자는 흰색 `#ffffff` |
| 위험/경고 | `#c62a2e` (빨강) |
| 다크 모드 배경 | `#1c1a15` (웜 다크) · 다크에서 포인트는 `#5fb87c` |

### 형태
| 요소 | 값 |
|---|---|
| 카드 모서리 | 8px 라운드 |
| 버튼 | **완전한 알약형(pill, 999px)** · 최소 높이 44px |
| 터치 타겟 | 전부 44×44px 이상 (모바일 우선) |
| 콘텐츠 최대 폭 | 1200px, 모바일은 좌우 20px 패딩 |
| 폰트 | **Pretendard** (한국어 산세리프) · 제목 bold |
| 목록 스타일 | 카드보다 **얇은 구분선 리스트**를 선호 (위아래 1px 선, 행 간 구분선) |

### 레이아웃 관습
- 상단: 로고 + 수평 내비 (모바일은 햄버거) / 하단: 푸터(연락처·YouTube 링크)
- 페이지 머리: 뒤로가기 링크(`← 관리` 형태) → 큰 제목 → 회색 한 줄 설명
- 관리 화면은 배지(`임원`/`전도사`)를 우상단에 표시

## 4. 사용자와 권한 — 3파트 (화면 기준)

| 파트 | 내부 role | 볼 수 있는 것 |
|---|---|---|
| **① 비로그인 방문자** (새가족) | GUEST | 홈·소개·예배안내·오시는길·말씀(YouTube)·공개 공지·주보·새가족 등록 신청 |
| **② 로그인 회원** | MEMBER | ① + 사진첩 열람·다운로드, 월례회 자료, 내 정보 |
| **③ 임원·전도사** | LEADER / PASTOR | ② + 모든 업로드·작성, **출석부**, 예산안, 회원 승인(전도사만) |

내부적으로 5단계: GUEST < PENDING(승인 대기) < MEMBER < LEADER < PASTOR.
회원 조직 단위는 "**마을**"(1~9마을 + 새가족)이다.

## 5. 전체 화면 목록 (라우트)

### A. 공개 영역
| 라우트 | 화면 |
|---|---|
| `/` | 홈 — 단체사진 히어로 + 클릭 게이트, 최신 공지·주보 미리보기 |
| `/welcome` | **처음 오시는 분** (최우선 페이지) — 예배 시간·장소, 가는 길, 새가족 환영 흐름 |
| `/welcome/register` | 새가족 등록 폼 — 이름·연락처(필수), 성별·나이대·유입경로(선택), 개인정보 동의 |
| `/about` | 소개 — 섬기는 사람들, 한 해의 흐름 |
| `/worship` | 예배와 모임 |
| `/sermons` | 말씀 — YouTube 설교 영상 목록 |
| `/bulletin` | 주보 — 최신 주보 이미지(장별), 지난 주보 목록 |
| `/news` · `/news/[slug]` | 소식(공지) 목록·상세 |
| `/location` | 오시는 길 — 지도, 주소, 카카오맵/네이버지도 버튼 |
| `/contact` | 문의 — 전화(사무실) |

### B. 인증·회원 영역
| 라우트 | 화면 |
|---|---|
| `/login` `/signup` `/signup/complete` `/pending` | 로그인·가입·승인 대기 |
| `/password/reset-request` `/password/reset` | 비밀번호 재설정 |
| `/my` | 나의 LIGHT (회원 홈, PWA 시작점) |
| `/my/profile` | 내 정보·비밀번호 변경·탈퇴 |
| `/photos` · `/photos/[id]` | **사진첩** — 앨범 그리드 → 사진 그리드 → 라이트박스(스와이프, 개별 다운로드, 신고) |
| `/meetings` · `/meetings/[id]` | 월례회 자료 — 열람 기간 제한, 페이지 단위 이미지 뷰어 |
| `/documents` · `/documents/[slug]` | 회의록·예산안 (임원 이상) |
| `/offline` | PWA 오프라인 폴백 |

### C. 운영(관리) 영역 — 임원 이상
| 라우트 | 화면 |
|---|---|
| `/admin` | 관리 홈 — 역할 배지, 승인 대기 알림(전도사), **저장 용량 게이지**, 기능 진입점 그룹 3개(콘텐츠/문서/관리) |
| `/admin/posts/new` · `/admin/posts/[id]/edit` | 공지·회의록·예산안 작성 (리치텍스트) |
| `/admin/bulletin/upload` | 주보 업로드 (이미지 여러 장, 순서 지정) |
| `/admin/albums/[id]/upload` | 사진 업로드 (드래그앤드롭, 병렬 업로드 큐) |
| `/admin/meetings/new` · `/admin/meetings/[id]` | 월례회 자료 업로드(PDF)·기간 관리·열람 로그 |
| `/admin/members` | 회원 관리 (전도사 전용) — 승인/거절/역할 변경 |
| `/admin/newcomers` | 새가족 등록 내역 (개인정보 1년 보유 안내) |
| `/admin/attendance` · `/admin/attendance/[id]` | **출석부 (신규)** — 아래 §6 상세 |

## 6. 출석부 — 가장 최근에 추가된 화면 (상세)

### 6a. 회차 목록 `/admin/attendance`
```
┌────────────────────────────────────┐
│  ← 관리                            │
│  출석부                            │
│  회차를 만들고 눌러서 출결 체크    │
├────────────────────────────────────┤
│  회차 만들기                       │
│  [날짜 ▾] [종류 ▾] [이름________]  │
│              [ 만들고 체크 시작 ]  │
├────────────────────────────────────┤
│  8/24 (일)  주일예배    체크 4/15  │
│  ──────────────────────────        │
│  8/17 (일)  주일예배    출석 11명  │
└────────────────────────────────────┘
```
- 만들기 폼이 목록 위에 인라인 (별도 페이지 없음)
- 행 우측은 진행 상태: 체크 중이면 "체크 4/15", 완료면 "출석 n명"

### 6b. 출석 체크 `/admin/attendance/[id]`
```
┌────────────────────────────────────┐
│  ← 출석부                          │
│  8/24 (일) 주일예배     체크 4/15  │
├────────────────────────────────────┤
│  1마을                             │
│  강OO •  [출석][지각][결석][공결]  │
│  김OO    [출석][지각][결석][공결]  │
│  2마을                             │
│  정OO    [출석][지각][결석][공결]  │
├────────────────────────────────────┤
│  저장 안 된 체크 2건      [ 저장 ] │ ← 하단 고정 바
└────────────────────────────────────┘
```
- 명단 전원을 **마을 그룹**으로. 한 사람당 상태 버튼 4개(출석=초록 채움,
  지각=초록 연함, 결석=빨강 채움, 공결=회색 채움, 미선택=외곽선만)
- 이름 옆 `•` = 아직 저장 안 된 변경. 하단 고정 바에 미저장 개수 + [저장]

## 7. API 명세 요약 (시각화용)

### 공통 규약
- Base: `https://<backend>/api` · JSON · 인증은 **httpOnly 쿠키** (access 30분 / refresh 14일)
- 성공: `{ "data": ... }` / 실패: `{ "error": { "code", "message", "field" } }`
- 에러 코드 7개: `UNAUTHORIZED`(401) `FORBIDDEN`(403) `PENDING_APPROVAL`(403) `NOT_FOUND`(404) `VALIDATION_ERROR`(400) `STORAGE_LIMIT`(409) `DUPLICATE`(409)
- 목록: `{ items, page, size, hasNext }` (사진만 커서 방식)
- ID는 문자열. 날짜는 ISO-8601 UTC
- 권한 표기: G(익명) / P(승인대기) / M(회원) / L(임원) / T(전도사) — 계단식 상위 포함

### 리소스 그룹 (섹션 구성)
| 그룹 | 경로 | 대표 엔드포인트 |
|---|---|---|
| 인증 | `/api/auth` | signup · login · logout · refresh · me · password 변경/재설정 |
| 게시물 | `/api/posts` | 공지(공개/내부)·회의록·예산안 CRUD — category로 구분, slug 상세 |
| 첨부 | `/api/attachments` `/api/files` | 업로드 · 다운로드(302 → presigned URL) |
| 주보 | `/api/bulletins` | latest · 목록 · 업로드(multipart) · 삭제 |
| 사진첩 | `/api/albums` `/api/photos` `/api/uploads` | 앨범 CRUD · 사진 커서 목록 · **presigned PUT 업로드** · 다운로드 · 신고 |
| 월례회 | `/api/meetings` | 목록·상세·페이지 이미지(열람 기간 제한)·업로드·열람 로그 |
| 관리 | `/api/admin` | 회원 목록/승인/거절/역할(T) · 저장 용량(L) · 새가족 내역(L) |
| 공개 | `/api/newcomers` `/api/sermons` | 새가족 등록 제출(G) · 설교 영상 목록(G) |
| **출석부 (신규)** | `/api/attendance` | 아래 상세 |

### 출석부 API (신규, 전부 권한 L)
| 메서드·경로 | 설명 |
|---|---|
| `GET /attendance/sessions?page=&size=` | 회차 목록. 각 항목에 `checkedCount`/`presentCount`/`rosterCount` 집계 |
| `POST /attendance/sessions` | 회차 생성 `{date, type, title}`. 같은 날짜+종류 중복 시 409 `DUPLICATE` |
| `GET /attendance/sessions/{id}` | 회차 + **명단 전원**의 출결. `status: null` = 기록 없음(결석과 다름) |
| `PUT /attendance/sessions/{id}/entries` | 출결 저장 — 본문은 배열 `[{rosterId, status}]`. **upsert**(보낸 것만 덮음) |
| `DELETE /attendance/sessions/{id}` | 회차 삭제 (204) |

- `status`: `PRESENT`(출석) `LATE`(지각) `ABSENT`(결석) `EXCUSED`(공결)
- 출결 대상은 계정이 아니라 **교인 명단(roster)** — 계정 없는 교인도 체크

### 인가 매트릭스 (개념)
행 = 엔드포인트, 열 = G/P/M/L/T, 셀 = 기대 HTTP 상태(200/401/403/404).
예: `POST /posts` → G:401, P:403, M:403, L:200, T:200.
공개 열람은 전부 200, 쓰기·관리·출석부는 L부터 200, 회원 관리는 T만 200.
예산안 상세는 권한 없으면 404(존재를 숨김).

## 8. 이미지 제작 시 지켜줄 것

1. **모든 UI 텍스트는 한국어** (위 화면 표의 문구 그대로 사용 가능)
2. 모바일 프레임 기준 390×844 (아이폰 비율), 데스크톱은 1200px 컨테이너
3. 색은 §3 값 그대로 — **포인트는 초록(#2f7a4a), 배경은 베이지(#f6f1e4)**. 파랑·보라 금지
4. 버튼은 알약형(pill), 카드는 8px 라운드, 리스트는 구분선 스타일
5. 사람 이름이 필요하면 가상 이름 사용 (김OO, 강하늘 등) — 실존 인물 금지
6. 사진 자리는 회색 placeholder로 (실제 인물 사진을 그려 넣지 말 것)
7. API 다이어그램은 §7의 envelope/에러코드/권한 열(G P M L T)을 포함하면 정확함
