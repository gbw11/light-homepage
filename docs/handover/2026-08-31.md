# 작업 인계 — 2026-08-31

**적는 순서: 못 한 것이 먼저, 한 것이 나중** (`README.md` 규칙).

오늘의 중심은 두 가지였다. **① 8/28에 "빨간 CI 채로 머지"됐던 원인 두 갈래를
규명하고 저장소 수준에서 없앴다** — 테스트 실패가 아니라 Jenkins 실행 환경과
Vercel 플랜 문제였다(§3.1). **② 로그인·권한 재설계가 BE 답변으로 확정되어,
스펙 3종 개정 + FE 재작업 5개 PR을 완성했다**(§3.2). FE PR 5개는 전부 CI
초록이지만 **아직 아무도 브라우저로 보지 않았다** — 그게 내일 첫 일이다.

---

## 1. 🔴 내일 먼저 볼 것

### 1.1 🔴 FE 인증 재작업 5개 PR 평가 — CI 초록 ≠ 화면이 맞다

#114→#118이 스택으로 쌓여 있다 (각각 이전 PR이 base — **#114부터 순서대로**
머지하면 GitHub가 다음 PR을 자동 재타겟한다). lint·type-check·mock build는
전부 통과했지만, HARNESS §2대로 **평가(사람 눈)는 별개다.**

`cd frontend && NEXT_PUBLIC_USE_MOCK=1 npm run dev` 후 (mock 계정 비밀번호는
전부 `password12!`):

| # | 시나리오 | 기대 결과 |
|---|---|---|
| 1 | `/signup` — `이믿음` / `2002-11-23` / `010-5555-6666` | 명단 확인 통과 → 5분 카운트다운 → 아이디·비번 설정 → "가입 완료" → 로그인 유도 |
| 2 | `/signup` — `김도연a` / `2001-03-14` / `010-1111-2222` (기가입) | **"명단에서 확인되지 않습니다" 단일 문구** — 어느 필드가 틀렸는지·기가입인지 알려주지 않아야 함 |
| 3 | `/signup` — `이중복` / `2000-01-01` / `010-7777-0000` | "동명이인 확인이 필요합니다. 임원에게 문의해 주세요" 별도 분기 |
| 4 | `/login` — `doyeon01` 로그인 | 아이디 필드(이메일 아님)·카카오 버튼 문구 "카카오로 로그인"(~~3초~~)·회원가입 링크 복구 확인 |
| 5 | `pastor1`로 `/admin/members` | 승인 대기 탭 없음 · 행별 [비번 초기화] → 코드(XXXX-XXXX)·만료 시각 표시 · [삭제] → 사유 입력 + "명단 재개방" confirm |
| 6 | 5에서 발급한 코드로 `/password/reset` | 재설정 성공 → 같은 코드 재사용 시 "올바르지 않거나 만료" |
| 7 | 5에서 `doyeon01` 삭제 → 로그아웃 → 시나리오 2 재시도 | 이번엔 **통과**해야 함 (명단 재개방 실측) |
| 8 | 로그아웃 상태로 `/photos` `/meetings` `/documents` | 🔒 로그인 유도 화면 (리다이렉트 아님) |
| 9 | 로그아웃 상태로 `/news` | 공개 공지만 + "회원 공지는 로그인하면" 안내 한 줄. 회원 공지 상세 URL 직접 진입 시 로그인 유도 |
| 10 | 회귀: `/` `/bulletin` `/worship` `/welcome` (로그아웃) | 여전히 공개로 열림 |

통과하면: **#114 → #115 → #116 → #117 → #118 순서로 머지** → 마지막에
`frontend_develop → develop` 통합 PR 하나 (그때 Vercel 프로덕션 반영).

### 1.2 🔴 Vercel 프로덕션 검증 + 옛 배포 삭제 — 8/29부터 이월 3일째

오늘 #108 머지로 **새 Production 배포가 생겨 §2.5의 전제 조건이 충족됐다.**
브라우저(SSO)로, 이 순서로:

1. **§2⓪ 커밋 대조** — Production Deployment 카드 커밋 = `origin/develop` HEAD
2. **§2② 자산 3종** — `/icons/icon-192.png` 200(먼저!) ·
   `/photos/retreat-2026/thumb/p001.webp` 404 · `/mock-assets/...p001.webp` 404
3. **§2.5 옛 배포 삭제** — `d0f8a0f`(사진 제거) 이전 배포 전부.
   ⚠️ Redeploy·Promote 금지 (`infra/vercel/README.md §2.5`)

같은 김에 **Neon 컴퓨트 그래프** — 8/27 누수 수정 후 빈 구간이 생겼는지
(`COST_GUARDRAILS.md §7`, "며칠 뒤 확인"의 그 확인) + 자동 정지 시간이 5분인지.

### 1.3 BE에게 전달할 것 (BACKEND_HANDOFF.md 2026-08-31 항목 2개)

1. **FE 가정 계약 4건 확인 요청** — register 201 쿠키 발급 여부 · 카카오
   registrationToken 전달 방식 · loginId 형식(FE: 영문 소문자·숫자 4~20자) ·
   리셋 코드 형식. FE는 가정으로 선행했고 수정 지점은 각 한 곳
2. **CI 체크 읽는 법** — Vercel `Canceled by Ignored Build Step`(초록)은 정상,
   Jenkins `cannot be built`는 고쳐짐. + **develop → backend_develop 동기화
   요청** (fix/** push 트리거·vercel.json이 내려가야 BE 브랜치에도 적용된다)

⚠️ BE PR의 Vercel X 처리 규칙은 오늘 PM이 별도로 확정했다 — `DECISIONS.md`
2026-08-31 "조건부로 무시" 항목 (backend_develop 한정 · develop 머지는 merge
commit 유지 조건).

---

## 2. 미확정 (막히지 않음, 대기만)

- **BE 답변 대기 4건** — §1.3의 가정 계약. SPEC_API에 ❓로 표기돼 있다
- **명단 CSV 전달 방법** — 원본은 저장소에 커밋하지 않는다 (BE §9 확인 사항)
- **가입 시 개인정보 동의 체크** — 처리방침 문구 확정과 함께 재검토
  (`WIREFRAME §10-2`)
- **admin 회원 목록의 마을 열** — 명단 DB에 마을 컬럼 확인되면 복원

---

## 3. 오늘 한 것

### 3.1 CI 가짜 빨강 두 갈래 제거 — 전부 머지·실측 완료

8/28 "빨간 채로 머지" 4건(#100·#102·#103·#104)의 원인 규명:

- **Jenkins `This commit cannot be built`** = 공식 이미지에 docker CLI 부재
  (Backend 스테이지 exit 127) + compose 네트워크 ↔ 기본 bridge 격리.
  → 커스텀 이미지 + `network_mode: bridge` (**#107**, `infra/jenkins/README.md
  §6.5`). 수정 후 전체 검증 빌드 SUCCESS로 증명
- **Vercel `Deployment was blocked`** = Hobby 플랜이 BE 계정 author를 차단.
  → `frontend/vercel.json` ignoreCommand (**#108**) — 비프론트 커밋은
  `Canceled by Ignored Build Step`(초록). 양방향 실측 완료.
  ⚠️ BE가 frontend/를 건드리면 여전히 X — 처리 규칙은 §1.3의 DECISIONS 항목
- **덤**: push 트리거가 `feat/**`뿐이라 `fix/*`·`docs/*` 브랜치는 push CI가
  안 돌던 구멍을 막음 (**#109**)

### 3.2 로그인·권한 재설계 확정 반영

BE가 §9 결정 7건에 답했고(A=아이디 · **B=즉시 MEMBER** · C=T만 · D=열람 M ·
E=권장안 · **F=카카오 유지** · G=동일 응답), 비호환 2건(village ·
PENDING_APPROVAL 제거) 합의됨.

- **#111** (머지됨): SPEC_API v1.3 · SPEC_FUNCTIONAL v1.3(§2.1 "타협 불가"
  뒤집은 근거 명시) · WIREFRAME v1.8 · DECISIONS 확정 기록
- **#113** (머지됨): 출석부 FE frontend_develop → develop 통합 (이월 4번).
  충돌(docs 재구성 vs 8/28 항목) 해소 포함
- **#114~#118** (CI 전부 초록, 평가 대기 — §1.1): 인증 계약 교체 → 가입
  2단계 → 리셋 코드 → 회원 관리 → 열람 게이트. mock 명단은 출석부와 통합
  (member_roster 하나가 원본)

### 3.3 구조 메모

- **스택 PR**: #115~#118은 앞 PR이 base다. 순서대로만 머지하면 된다
- `/news/[slug]`는 ISR이라 회원 공지 본문을 정적 HTML에 굽지 않도록 서버
  401 → 클라이언트 fetch 분기 구조를 넣었다 (`MemberNoticeDetail`) — 통합
  때 실백엔드로 이 경로를 꼭 실측할 것 (서버 익명 fetch가 401을 받는 것이
  정상 경로다)
- `real.ts` NO_REFRESH_PATHS에 익명 인증 경로 3개 추가 — 명단 불일치 401이
  "세션 만료"로 오인돼 가입자가 로그인으로 튕기는 사고 방지
