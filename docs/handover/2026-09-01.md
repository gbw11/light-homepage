# 작업 인계 — 2026-09-01

**적는 순서: 못 한 것이 먼저, 한 것이 나중** (`README.md` 규칙).

오늘은 **이월이 없다.** 8/29부터 5일간 밀려온 Vercel 검증과 8/27 누수 확인이
둘 다 닫혔고, 어제 남긴 FE 인증 5개 PR도 평가·통합을 마쳤다. 남은 것은 전부
**남이 답해야 하는 것**(BE)과 **PM 판단 1건**이다.

---

## 1. 🔴 내일 먼저 볼 것

### 1.1 BE 전달 — 이슈 [#133](../../issues/133)

오늘 만든 이슈 하나에 4건을 모았다. **전달만 하면 된다.** 우선순위 순:

| # | 해줄 것 | 왜 급한가 |
|---|---|---|
| 1 | **PR #123 머지** (`develop → backend_develop`) | BE 브랜치 4개가 지금 전부 Vercel `Blocked`. `git.deploymentEnabled`는 **배포되는 브랜치의 `vercel.json`**에서 읽히는데 `backend_develop`이 뒤처져 있다 |
| 2 | **`GET /api/sermons/live` 구현** | 주일 라이브 자동 표시. FE는 mock으로 완성, BE 없이는 동작 안 함 |
| 3 | FE 가정 계약 4건 **확인만** | 다르면 FE 수정 지점은 각각 한 곳 |
| 4 | `GET /api/sermons` 참고 | 해줄 것 없음 (선택: 15편 상한 넘기면 더 좋음) |

⚠️ **1번을 미루면 BE 브랜치마다 빨간 X가 계속 쌓인다.** 오늘 오전에 원인을
규명했고(#120이 틀린 게 아니라 도달하지 않은 것), 조치는 PR 하나 머지가 전부다.

### 1.2 🟡 PM 판단 1건 — 채널 RSS가 프로덕션에서 막혀 있다

`/sermons/feed`(채널 RSS를 읽는 임시 라우트)가 배포 환경에서 **4회 연속 502**다.
로컬 curl은 항상 200이라 **데이터센터 IP 제한**으로 보인다.

- **지금 깨진 것은 없다** — 하드코딩 스냅샷(14편)으로 폴백해 화면·썸네일 정상
- 다만 **자동 갱신이 사실상 동작하지 않는다.** 새 설교가 올라와도 스냅샷 그대로

선택지 셋:

1. **그냥 둔다 (권장)** — BE가 `GET /api/sermons`를 구현하면 이 라우트는 지운다
   (이슈 #133 항목 4). 그때까지 스냅샷으로 화면은 멀쩡하다
2. **YouTube Data API 키 발급** — 키가 있으면 IP 차단을 안 받는다. Google Cloud에서
   키를 만들어주면 FE가 붙인다. 쿼터 관리·키 보관이 따라온다
3. **스냅샷 주기적 수동 갱신** — 채널을 확인해 `mock.ts`의 목록을 갱신

### 1.3 🟡 눈으로 확인 못 한 것 2건

- **모바일 폭** — 오늘 레이아웃을 크게 바꿨는데(헤더 한 줄·양옆 레일·푸터)
  창 리사이즈가 먹지 않아(`innerWidth`가 1440에서 안 변함) **반응형 클래스만
  확인**했다. 휴대폰이나 devtools로 한 번 봐야 한다 — 특히 햄버거 메뉴와
  레일이 안 보이는지
- **다크 모드** — 팔레트를 통째로 바꿨는데(#129) OS 스킴을 못 바꿔서 **토큰 값
  파싱과 대비 계산으로만** 확인했다. 라이트/다크 각 12쌍 24개 조합은 전부 AA를
  넘지만(최저 4.63) 눈으로 본 것은 아니다

### 1.4 첫 화면 워드마크 — 실제로 보고 판단할 것

PM 요청으로 스크림(회색 판)을 걷어냈다. 워드마크가 **하필 사진 속 밝은
프로젝션 스크린과 겹치는 자리**라 그림자 3겹으로 읽히게 만들었다. 스크린샷상
읽히지만, 실제로 답답하면 선택지가 있다:

- 글자를 스크린 밖(아래쪽 어두운 영역)으로 내리기
- 아주 옅은 그라데이션을 **글자 뒤에만** (화면 전체는 그대로)
- 가운데가 밝지 않은 다른 사진으로 교체

⚠️ **사진을 교체하면 가운데 영역 밝기를 다시 확인해야 한다** — 코드 주석에도
남겼다 (`LandingGate.tsx`).

---

## 2. 미확정 (막히지 않음, 대기만)

- **BE 답변 대기 4건** — 이슈 #133 항목 3 (register 쿠키 · registrationToken
  전달 · loginId 형식 · 리셋 코드 형식). `SPEC_API`에 ❓로 표기돼 있다
- **명단 CSV 전달 방법** — 원본은 저장소에 커밋하지 않는다 (BE §9 확인 사항)
- **가입 시 개인정보 동의 체크** — 처리방침 문구 확정과 함께 재검토
- **admin 회원 목록의 마을 열** — 명단 DB에 마을 컬럼 확인되면 복원
- **Instagram 핸들** — `links.ts`가 아직 `null` (`PLAN §8`). 값이 들어오면
  헤더 메뉴·푸터·왼쪽 레일 세 곳이 함께 살아난다
- **§2.5 옛 배포 삭제** — 대상 preview 5건을 확정해 `infra/vercel/README.md`에
  표로 남겼다. **PM 결정으로 오늘은 실행하지 않았다.** 보호를 풀기 전에 실행

---

## 3. 오늘 한 것

### 3.1 🔴 어제의 스택 머지 사고 회수

8/31에 #114~#118을 10초 안에 연달아 머지하면서 **재타겟이 따라오지 못했다.**
#114만 `frontend_develop`에 들어갔고 나머지 4개는 각자의 feature 브랜치에
머지돼 있었다 — PR 목록에는 전부 `MERGED`, CI도 초록이라 겉으로는 멀쩡했다.

- **#125** 회수 (유실 0건 확인 — `develop`에 없는 파일 0개)
- **#126** `frontend_develop → develop` 통합 (충돌 1곳: `BACKEND_HANDOFF.md`,
  양쪽 최신 항목을 시간순으로 둘 다 보존)
- **#122** 재발 방지 — `DECISIONS.md` + **`HARNESS.md §4.4`**(머지 → 재타겟
  화면 확인 → 다음, 그리고 머지 후 산출물 검증 명령)
- 오늘 이후 스택 PR(#130→#131)은 **정상 동작을 확인**했다

### 3.2 시나리오 10건 평가 — 전부 통과 (mock 결함 2건 수정 후)

어제 "CI 초록 ≠ 화면이 맞다"로 남긴 평가를 끝냈다. 8건 즉시 통과, 2건이 결함을
드러냈다. **둘 다 화면이 "성공"이라 말한 뒤 다음 한 걸음이 조용히 어긋나는** 종류:

- `resetPasswordWithCode`가 **새 비밀번호를 저장하지 않았다** — 전역 상수
  하나와 대조해서, "변경되었습니다" 직후 새 비밀번호 로그인이 실패
- `deleteMember`가 **시드 계정을 못 지웠다** — 에러 없이 성공 처리되고 목록에
  남는데 명단 `claimedBy`만 해제돼 상태가 어긋남

수정 후 재실측: 재설정 → 새 비번 로그인 성공 · 옛 비번 거부 · 삭제 후 3명→2명 ·
재가입(`doyeon02`) 후 로그인까지 완주. **ISR 유출도 없다** — 회원 공지 상세의
서버 HTML(19KB)에 제목도 본문도 없다.

### 3.3 ✅ 이월 2건 종결 (#127)

- **Vercel §2⓪·§2②** — 커밋 대조 `959016c` 일치 · 자산 3종 기대값과 정확히 일치.
  5일이나 걸린 이유는 **SSO 302**였고, **브라우저 안에서 same-origin `fetch`**로
  뚫었다. 재현 방법을 `§2.6`에 남겼다 (⚠️ vercel.com 탭에서 실행하면 CORS로 실패)
- **Neon** — Primary `SUSPENDED` **19시간째**. 8/27 누수 3건이 실제로 막혔다는
  **유일한 증거**를 확보했다. 자동 정지 5분도 확인

### 3.4 #120 실측 — 결론은 "아직 안 됨", 원인 규명 완료

BE 브랜치 Vercel X가 그대로였다. **`git.deploymentEnabled`는 배포되는 브랜치의
`vercel.json`에서 읽힌다** — `backend_develop`이 뒤처져 그 설정이 없었다.
어제 폴백으로 적어둔 "조건부 무시"는 **필요 없다**(②가 틀린 게 아니라 도달하지
않은 것). 조치는 PR #123 머지 (→ §1.1).

### 3.5 UI·디자인

| PR | 내용 |
|---|---|
| **#128** | `window.confirm` **8곳**(3곳인 줄 알았다) → 공용 `ConfirmDialog`. 초기 포커스 취소 · 포커스 트랩 · Escape. 헤더 CTA 로그인 상태 반영. 탭 제목 중복 13곳 제거 |
| **#129** | 디자인 톤 **STUDIO FLEUR** 팔레트로 교체. 원색 4개가 명도 70% 이상이라 표면에만 쓰고 글자·CTA는 같은 색조에서 명도만 옮겼다. **24개 조합 전부 AA(최저 4.63)** |
| **#130 · #131** | `/sermons` 라이브 우선 재구성 + `/sermons/all` 지난 말씀(한 행 4개). **썸네일이 안 뜨던 원인 = mock이 존재하지 않는 영상 id를 쓰고 있었다** → 실제 채널 영상으로 교체 |
| **#132** | 채널 RSS 간헐 404 대응 (실패가 30분 굳던 것 + 폴백이 세션을 지배하던 것) |
| **#134** | 사이트 뼈대를 모교회(`gloria.or.kr`)에 맞춤 — 헤더 한 줄 가로 메뉴 · 양옆 고정 레일 · 푸터 · 예배 안내 표. **색은 우리 팔레트 유지** |

### 3.6 구조 메모

- **`CHURCH_SITE_URL` 확정** (`https://www.gloria.or.kr`) — 그동안 `null`이라
  푸터의 "김해교회 홈페이지" 링크가 **아예 그려지지 않고 있었다.** `PLAN §8`의
  ❓ 하나가 닫혔다
- **`/sermons/feed`는 `/api/` 밖에 뒀다** — `next.config.ts`가 `/api/:path*`를
  Spring으로 rewrite한다. 경로로 미리 떼어놓는 편이 나중에 원인을 찾는 것보다 싸다
- **`navigation.ts` 신설** — 메뉴 구조를 데스크톱·모바일이 공유한다. 회원 전용
  자료는 공개 메뉴와 줄을 나눴다
- **양옆 레일은 `xl`(1280px)부터** — 컨테이너가 `max-w:1200px`라 `lg`에서는
  좌우 여백이 0이고 44px 레일이 본문 글자를 가린다
