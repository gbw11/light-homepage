# 작업 인계 — 2026-08-24

**내일 이 파일부터 읽는다.** 오늘 끝낸 것보다 **끝내지 못한 것**이 먼저 온다.

- 오늘 머지 상태: `frontend_develop` = `aa8c2f8` · `develop` = `d3120f2`
- 오늘 완료: **M3 FE 전체** (사진 대량 업로드 · 주보 업로드/교체/삭제)
- 열려 있는 `feat/*` 브랜치: **없음** (머지 후 삭제 — `DECISIONS.md` 2026-08-24)

---

## 1. 🔴 가장 먼저 — 브라우저 육안 검증 (오늘 못 한 것)

오늘 만든 두 기능은 **lint · type-check · `mock=0` 프로덕션 빌드 · 라우트 200까지만
확인했다.** 브라우저에서 실제로 동작하는 걸 **사람이 눈으로 본 적이 없다** (작업
환경에 브라우저 자동화가 없다).

`frontend/docs/WORKFLOW.md §2`는 "화면에서 눈으로 확인"을 검증 루프의 필수 단계로
두고 있다. **이 검증을 통과하지 못한 채 머지된 상태**라는 것을 알고 시작해야 한다.

```bash
cd frontend && npm run dev     # http://localhost:3000
```

로그인: `leader@example.com` / `password12!` (비밀번호는 모든 mock 계정 공통)

### 1-1. 사진 대량 업로드 (FR-PHO-08)

| # | 경로 | 봐야 하는 것 |
|---|---|---|
| 1 | `/my/photos` → "2026 여름 수련회" | 헤더에 **[사진 올리기]** 가 보이는지 (임원 이상에만) |
| 2 | → `/admin/albums/5/upload` | 드롭존 · [파일 선택] · 2560px 안내 문구 |
| 3 | 사진 여러 장 선택 | 목록이 `준비 중` → `대기`로 바뀌는지, 미리보기 없이 이름·상태만 뜨는지 |
| 4 | **[N장 업로드]** | 진행률 바가 **움직이는지**, `올리는 중` → `완료` |
| 5 | 앨범으로 돌아가기 | **방금 올린 사진이 그리드에 실제로 보이는지** (mock이 objectURL로 붙잡아 둔다) |
| 6 | `?mock=upload-fail` 붙이고 재시도 | photoId 4의 배수만 실패 → 행마다 **[재시도]**, 2건 이상이면 **[실패 N건 모두 재시도]** |
| 7 | `?mock=storage` | 재시도 유도가 아니라 **"관리 화면에서 용량 정리"** 안내가 뜨는지 |
| 8 | 업로드 중 탭 닫기 시도 | 브라우저 이탈 경고가 뜨는지 |

⚠️ **HEIC 파일이 있으면 함께 넣어볼 것.** `건너뜀`으로 분리되고 이유가 표시돼야
한다 (재시도 대상에 들어가면 버그다).

### 1-2. 주보 업로드 (FR-BUL-05/06)

| # | 경로 | 봐야 하는 것 |
|---|---|---|
| 1 | `/my/bulletin` | 상단에 **[주보 올리기] · [이 주보 삭제]** 줄 (임원 이상에만) |
| 2 | → `/admin/bulletin/upload` | 날짜 입력 · [+ 선택] |
| 3 | 이미지 2장 이상을 **일부러 뒤집힌 순서로** 선택 | 미리보기 · 번호 · ↑↓ · ✕ |
| 4 | ↑↓ 로 순서 바로잡기 / 행을 **끌어서** 옮기기 | 번호가 따라 바뀌는지 |
| 5 | 날짜를 **기존과 다른 날짜**로 두고 [업로드] | `변환 중 1/2` → `올리는 중` → 성공 안내 |
| 6 | `/my/bulletin` | **올린 순서대로** 1→2 페이지가 나오는지 ← **이 기능의 전부다** |
| 7 | 날짜를 `2026-08-24`(기존 존재)로 두고 [업로드] | "교체할까요?" confirm → 교체되는지 |
| 8 | [이 주보 삭제] | 날짜가 들어간 confirm → 삭제 후 다음 주보로 넘어가는지 |

**문제가 보이면 브랜치를 새로 판다** (`feat/fe-photo-upload-fix` 같은 이름).
지운 브랜치를 되살리지 않는다 — `DECISIONS.md` 2026-08-24.

---

## 2. 🟡 BE 없이는 끝낼 수 없는 것

| 항목 | 왜 막혔는지 | 참고 |
|---|---|---|
| 사진 업로드 **실제 R2 왕복** | 브라우저 → R2 직접 PUT이라 mock으로 검증 불가. presigned 서명·버킷 CORS가 맞아야 한다 | [handoff/photo-upload](../handoff/2026-08-24-photo-upload.md) |
| 주보 **실제 multipart 왕복** | Spring이 파트 순서를 보존하는지 확인 필요 | [handoff/bulletin-upload](../handoff/2026-08-24-bulletin-upload.md) |
| ZIP 다운로드 (FR-PHO-05) | mock이 ZIP을 만들 수 없다 (`capabilities.zipDownload=false`로 한계를 화면에 명시함) | |
| 주보 목록 페이지네이션 (FR-BUL-02) | mock이 `hasNext: false`라 [더 보기]가 눌리지 않는다 | |
| 401 자동 갱신 (M2) | 실제 만료 왕복이 필요하다 | |

## 3. ⏳ 남이 답해야 진행되는 것

| # | 대기 중인 결정 | 누가 |
|---|---|---|
| 1 | `SPEC_API §6.5` **재시도 시 photoId 재발급** — 요청 스키마에 `photoId` 자리가 없다. 지금대로면 R2에 고아 객체가 남는다 | BE |
| 2 | `SPEC_API §5.4` **주보 교체** — 교체 엔드포인트가 없어 "삭제 → 재업로드"로 구현했다. 중간 실패 시 기존 주보가 사라진다 | BE |
| 3 | multipart 파트 이름 `pages` vs `pages[]` | BE |
| 4 | **브랜드 초록 CTA 대비 미달** (네이비 3.85:1 / 흰색 4.02:1, 기준 4.5:1). 중간 명도라 텍스트 색만으로 해결 불가 — 색을 바꿀지, 예외로 둘지 | **PM** |
| 5 | 월례회 화면 문구를 와이어프레임 원문에서 바꿨다 ("캡처 차단 불가"를 명시) — 승인 여부 | **PM** |

## 4. 📋 남은 FE 작업 (M4)

우선순위 순.

1. **글 수정 화면** `/admin/posts/[slug]/edit` (~4h) — 지금은 작성만 되고 수정이 안 된다
2. **월례회 업로드 + 기간 수정 · 열람 로그** (관리자, ~4h) — 뷰어만 있고 관리 화면이 없다
3. **성능 최적화** Lighthouse 90+ (6h) — 화면이 다 붙은 지금이 측정 적기
4. 접근성: 사진 라이트박스·주보 뷰어 **핀치 줌** (현재 브라우저 기본 동작만)

## 5. 🧹 리포 위생 (짧게 처리 가능)

- [ ] **`frontend_develop` ↔ `develop` 문서 동기화.** 오늘 문서가 양쪽에 나뉘었다 —
      `frontend_develop`에 `SPEC_FUNCTIONAL`·`WORKPLAN`·`BACKEND_HANDOFF`·`DECISIONS`
      갱신분, `develop`에 `docs/handoff/*`. `INTEGRATION.md §6.6`이 경고한 바로 그
      상황("내 브랜치에는 있는데 상대 브랜치에는 없는 문서")이다
- [ ] **`worktree-agent-*` 로컬 브랜치 13개 정리.** 과거 작업 잔여물이고 원격에는
      없다. `git branch -d`로 지워지지 않으면 머지 여부를 먼저 확인할 것
- [ ] ⚠️ **`public/`의 mock 사진 자산은 인증 없이 접근 가능하다.** 첫 공개 배포
      전에 번들에서 제외해야 한다 (`CICD.md §5.0`) — 초상권 문제로 이어진다

---

## 6. 오늘 한 것 (참고)

| 기능 | 커밋 | 상태 |
|---|---|---|
| 사진 대량 업로드 (FR-PHO-08) | 7개 단위 커밋 → `0c53b57` 머지 | 🟡 육안 검증 대기 |
| 주보 업로드·교체·삭제 (FR-BUL-05/06) | 5개 단위 커밋 → `aa8c2f8` 머지 | 🟡 육안 검증 대기 |
| BE 인계 브리핑 2건 | `317e853` · `d3120f2` (develop) | ✅ |
| 브랜치 정리 정책 재확인 | `DECISIONS.md` | ✅ |

새로 생긴 재사용 자산:

- `frontend/src/lib/image/resize.ts` — `resizeForUpload`(사진 2종) ·
  `resizeToWebp`(단일, 주보에서 사용). EXIF 회전은 `imageOrientation: "from-image"`
- `frontend/src/lib/image/exif.ts` — 촬영 시각만 읽는 최소 EXIF 파서 (의존성 없음)
- `queue.ts`의 `runWithConcurrency` — 동시 실행 수 제한. 다른 대량 작업에도 쓸 수 있다
- mock 시나리오 `?mock=upload-fail` 추가 (`frontend/README.md` 표에 기록)
