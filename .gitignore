# ─────────── 공통 ───────────
.DS_Store
Thumbs.db
*.log
*.tmp

# Claude Code 에이전트 워크트리/세션 상태 (git worktree 병렬 작업용)
.claude/

# 환경 변수 / 시크릿  ★ 절대 커밋 금지
.env
.env.local
.env.*.local
*.pem
*.key

# ─────────── frontend (Next.js) ───────────
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/build/
frontend/.vercel/
frontend/next-env.d.ts
frontend/*.tsbuildinfo
frontend/coverage/

# ─────────── backend (Spring Boot / Gradle) ───────────
backend/build/
backend/.gradle/
backend/bin/
backend/out/
backend/!**/src/main/**/build/
backend/!**/src/test/**/build/

# 시크릿 설정  ★ 절대 커밋 금지
backend/src/main/resources/application-local.yml
backend/src/main/resources/application-secret.yml
backend/src/main/resources/application-prod.yml

# ─────────── IDE ───────────
.idea/
*.iml
*.ipr
*.iws
.vscode/
!.vscode/extensions.json
.settings/
.project
.classpath

# ─────────── OS / 기타 ───────────
node_modules/
*.swp
*~

# DB 덤프 — ⚠️ 개인정보다 (members · newcomer_requests). 절대 커밋하지 않는다.
#   백업·복원 절차: infra/neon/README.md §3
*.dump
*.sql.gz
light-[0-9]*.sql

# ─────────── 로컬 스크래치 ───────────
# PM PC에서 문서·키 정리하며 만든 임시 파일. 키 목록이 섞여 있을 수 있어
# 절대 커밋하지 않는다. 필요 없어지면 그냥 지우면 된다.
scratch_*.txt
pm_chunks.json
